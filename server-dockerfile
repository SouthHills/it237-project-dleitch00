FROM node:22.22.0

WORKDIR /application

COPY server /application/server

WORKDIR /application/server

RUN npm install

EXPOSE 3000

CMD ["node", "--loader", "ts-node/esm", "src/server.ts"]

RUN echo "server started at http://localhost:3000/"
